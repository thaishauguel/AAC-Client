import React from 'react'

const Loading = (props) => {
    return (
        <div className="Loading">
            <h1>{props.text} is<br/> coming<br/>...</h1>
        </div>
    )
}

export default Loading
